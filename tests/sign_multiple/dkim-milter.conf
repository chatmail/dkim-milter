socket = inet:127.0.0.1:0
signing_senders = <tests/sign_multiple/signing-senders
signing_keys = <tests/sign_multiple/signing-keys
recipient_overrides = <tests/sign_multiple/recipient-overrides

canonicalization = relaxed
oversign_headers = From
