socket = inet:127.0.0.1:0
signing_senders = tests/sign_both/signing-senders
signing_keys = tests/sign_both/signing-keys
recipient_overrides = tests/sign_both/recipient-overrides

[signing]
canonicalization = relaxed
